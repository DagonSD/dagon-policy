apiVersion: cli.kyverno.io/v1alpha1
kind: Test
metadata:
  name: require-readonly-rootfs-test
policies:
  - policy.yaml
resources:
  - resources.yaml
results:
  - policy: require-readonly-rootfs
    rule: check-readonly-rootfs
    resource: pod-writable-rootfs
    namespace: team-alpha
    kind: Pod
    result: fail
  - policy: require-readonly-rootfs
    rule: check-readonly-rootfs
    resource: pod-missing-rootfs-setting
    namespace: team-alpha
    kind: Pod
    result: fail
  - policy: require-readonly-rootfs
    rule: check-readonly-rootfs
    resource: pod-readonly-rootfs
    namespace: team-alpha
    kind: Pod
    result: pass
